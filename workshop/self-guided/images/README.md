# Place screenshot assets for the self-guided lab pages here.
#
# Reference them from page markdown with a relative path, e.g.:
#   ![](./images/<file>.png)
#
# This folder is the single source of truth for screenshots. The Skillable
# track (workshop/skillable/) reuses these same files via absolute raw
# GitHub URLs rather than keeping a second copy — so update images here only.
#
# TODO(image-recapture): after the instructions refactor (concierge prompt now
# loaded from instructions/concierge.md, deployed baseline = instructions-0),
# re-capture the Observe prompt/trace/Monitor screenshots against that baseline
# so the agent responses shown match what learners actually get.
